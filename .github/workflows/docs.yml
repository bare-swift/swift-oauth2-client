name: Publish docs

on:
  push:
    tags: ['v*']
  workflow_dispatch:

jobs:
  publish:
    uses: bare-swift/bare-swift/.github/workflows/publish-docs.yml@main
    with:
      target: OAuth2Client
    permissions:
      pages: write
      id-token: write
      contents: read
